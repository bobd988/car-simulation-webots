plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile files("/Applications/Webots.app/lib/controller/java/Controller.jar")
    compile files("/Applications/Webots.app/lib/controller/java/vehicle.jar")
}
